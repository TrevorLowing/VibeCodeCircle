# vibecode-deploy testing

## Test data

This plugin’s test fixtures are synced from the CFA repo:

- CFA repo root `*.html`
- `css/`
- `js/`
- `resources/`

Fixtures are stored in:

- `testing/plugins/vibecode-deploy/data/fixtures/cfa-root/`

## Sync

Run:

- `testing/plugins/vibecode-deploy/scripts/sync-from-cfa.sh`
